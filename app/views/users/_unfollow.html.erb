<%= form_for(current_user.active_relationships.build, html: { method: :delete }) do |f| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= f.submit t("users.unfollow"), class: "btn" %>
<% end %>
